import '../validicityserver.dart';
import '../model/organisation.dart';

/// A fully managed Controller for Organisations.
class OrganisationController extends ManagedObjectController<Organisation> {
  OrganisationController(ManagedContext context) : super(context);
}
